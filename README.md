# server-healthcheck

Tiny utility to ping a list of hosts and report which are reachable.

## Usage

```bash
python check.py example.com google.com
```

## Config

Set `API_KEY` in `check.py` to enable status reporting to your dashboard.
